# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual Environment
venv/
env/
ENV/
.venv

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Claude Code
.claude/settings.local.json

# Environment variables
.env
.env.local

# API Keys (security)
*.key
*_key.txt
credentials.json

# OS
.DS_Store
Thumbs.db

# Logs
*.log

# Database
*.db
*.sqlite
*.sqlite3

# Chroma DB (if used for RAG)
chroma_db/
chromadb/

# Office temp/lock files
~$*

# Internal instructor reference — never commit (self-contained run of show)
RUN-OF-SHOW.md

# Participant-facing Anna assessment page — delivered directly, not via the repo
anna-investigation.html

# ── Slide sources (2026-08-19) ───────────────────────────────────────────────
# The hand-edited .pptx decks are the source of truth. The .md render sources
# have diverged from them, so a re-render from .md would silently clobber the
# PowerPoint edits. Kept LOCALLY (still on disk, diff_deck_vs_source.py still
# works) but removed from the repo so nobody renders from a stale source.
# Last tracked state is preserved in git history at commit 2b277e2.
blackhat-2026-adversarial-ai/*/slides/*.md

# (No exceptions. M9's m9.md exception was removed 2026-08-24 once its deck was
# rendered and hand-edited — the .md is now out of sync like all the others.)

# Build spec for slides that were never rendered into any deck. Kept locally —
# it is the only copy of that work — but not published. Last tracked content is
# in history at 2b277e2 if it is ever needed back.
blackhat-2026-adversarial-ai/coverage-slides.md

# Backups written by _templates/fix_code_blocks.py
*.pptx.bak
