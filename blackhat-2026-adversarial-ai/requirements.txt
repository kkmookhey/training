# Shared venv for the concept demos - install into ./.venv (Python 3.14 OK).
# Pinned to the exact versions verified end-to-end on 2026-07-30.
#
#   python3 -m venv .venv
#   source .venv/bin/activate        # Windows: .venv\Scripts\activate
#   pip install -r requirements.txt
#
# Covers M3 (RAG), M5 (agent), and M6 (MCP). M7 (crewAI) uses its OWN venv at
# M7-multi-agent/code/venv - crewAI's pins conflict with this stack (see that
# module's requirements.txt).

# --- M3 RAG (ingest + bot) ---
langchain==1.3.14
langchain-text-splitters==1.1.2
langchain-community==0.4.2
langchain-ollama==1.1.0
langchain-chroma==1.1.0
langchain-huggingface==1.2.2
sentence-transformers==5.6.1
chromadb==1.5.9
pypdf==6.14.2

# --- M5 agent (create_agent is LangGraph-based) ---
langchain-core==1.5.3
langgraph==1.2.10

# --- M6 MCP (1.x keeps the decorator API + fastmcp; 2.0.0 removed them) ---
mcp==1.29.0

# --- M5 optional Streamlit UI (the CLI is the primary path) ---
streamlit==1.60.0

# BYOK option (Day-2 frontier models), install only if you set OPENAI_API_KEY:
# langchain-openai
