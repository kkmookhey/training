# FAKE secrets - demo prop for the M7 privilege-escalation leak. Not real.
# The privesc demo reads a file named `.env`. Real `.env` files are gitignored,
# so this shipped copy is the source: `cp .env.example .env` before Attack 2.
DATABASE_URL=postgres://admin:hunter2-not-real@db.internal:5432/widgets
STRIPE_SECRET_KEY=sk_live_REDACTED_fake_demo_not_a_real_key
JWT_SIGNING_SECRET=demo-not-a-real-signing-secret-000
